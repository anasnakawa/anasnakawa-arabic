<!-- displaying a list of tags with their corresponding links -->
  <% for tag, index in @tags: %>
    <a href="<%= '/tags/' + tag + '.html' %>">
      <span class="label app-tag"><%= @map[ tag ] %></span>
    </a>
  <% end %>