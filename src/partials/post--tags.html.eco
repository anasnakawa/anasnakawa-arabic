<!-- displaying a list of tags with their corresponding links -->
<% for tag, index in @tagsAr: %>
  <a href="<%= '/tags/' + @tags[ index ] + '.html' %>">
    <span class="label app-tag"><%= tag %></span>
  </a>
<% end %>